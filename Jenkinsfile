@Library('Utilities')
import asu.selfservice.Utilities
def u = new Utilities()

pipeline {
  agent any
  stages {
    stage('Dev: HelloWorld') {
      steps {
        u.HelloWold('Brian is Amazing'
     }
    }
  }
}
